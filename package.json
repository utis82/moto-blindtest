{
  "name": "moto-blindtest",
  "version": "1.0.0",
  "description": "Moto Blindtest - Jeu de reconnaissance de sons de motos",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "npm run build:frontend && npm run build:backend && npm run prisma:generate",
    "build:frontend": "cd app/frontend && npm install && npm run build",
    "build:backend": "cd app/backend && npm install && npm run build",
    "prisma:generate": "cd app/db && npx prisma generate",
    "start": "cd app/backend && node dist/backend/src/index.js",
    "postinstall": "npm run build"
  },
  "keywords": ["moto", "blindtest", "game"],
  "author": "",
  "license": "ISC"
}
